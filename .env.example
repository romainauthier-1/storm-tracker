# URL du backend Storm Tracker (préfixe EXPO_PUBLIC_ = exposé au client).
EXPO_PUBLIC_BACKEND_URL=http://localhost:3000

# Clé publique VAPID pour les notifications push web (PWA).
EXPO_PUBLIC_VAPID_PUBLIC_KEY=
